# Используем официальный образ Python 3.9 (или другую нужную версию)
FROM python:3.9-slim

# Отключаем буферизацию вывода, чтобы логи сразу отправлялись в stdout
ENV PYTHONUNBUFFERED=1

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл зависимостей и устанавливаем их
COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

# Копируем весь исходный код проекта в рабочую директорию контейнера
COPY . /app/

# Указываем команду запуска бота (замените "run.py" на имя основного файла вашего бота)
CMD ["python", "run.py"]
